import { lightColors, palettes } from './colors';
import { fontFamily, fontSize, textStyles } from './typography';
import { spacing, screenPadding, radius } from './spacing';
import { shadows } from './shadows';

export const theme = {
  colors: lightColors,
  palettes,
  fontFamily,
  fontSize,
  textStyles,
  spacing,
  screenPadding,
  radius,
  shadows,
};

export { lightColors, palettes, fontFamily, fontSize, textStyles, spacing, screenPadding, radius, shadows };

export default theme;
